import { getDeployedInfraContract } from "../../../../contract/deployment/utils/infra.js";
import { uninstallExtension } from "../../__generated__/ModularCore/write/uninstallExtension.js";
export function uninstallPublishedExtension(options) {
    const { client, chain, contract, extensionName, publisherAddress, constructorParams, extensionData, } = options;
    return uninstallExtension({
        contract,
        asyncParams: async () => {
            const deployedExtension = await getDeployedInfraContract({
                chain,
                client,
                contractId: extensionName,
                constructorParams: constructorParams || [],
                publisher: publisherAddress,
            });
            return {
                extension: deployedExtension?.address,
                data: extensionData || "0x",
            };
        },
    });
}
//# sourceMappingURL=uninstallPublishedExtension.js.map