import { getContract, } from "../../../../contract/contract.js";
import { resolveImplementation } from "../../../../utils/bytecode/resolveImplementation.js";
import { uninstallExtension } from "../../__generated__/ModularCore/write/uninstallExtension.js";
export function uninstallExtensionByProxy(options) {
    const { client, chain, contract, extensionProxyAddress, extensionData } = options;
    return uninstallExtension({
        contract,
        asyncParams: async () => {
            const extensionImplementation = await resolveImplementation(getContract({
                chain,
                client,
                address: extensionProxyAddress,
            }));
            return {
                extension: extensionImplementation.address,
                data: extensionData || "0x",
            };
        },
    });
}
//# sourceMappingURL=uninstallExtensionByProxy.js.map