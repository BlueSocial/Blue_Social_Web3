"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.useConnect = void 0;
const useConnect_js_1 = require("../../../core/hooks/wallets/useConnect.js");
const index_js_1 = require("../../index.js");
/**
 * A hook to set a wallet as active wallet
 * @returns A function that lets you connect a wallet.
 * @example
 * ```jsx
 * import { useConnect } from "thirdweb/react";
 * import { createWallet } from "thirdweb/wallets";
 *
 * function Example() {
 *   const { connect, isConnecting, error } = useConnect();
 *   return (
 *     <button
 *       onClick={() =>
 *         connect(async () => {
 *           // instantiate wallet
 *           const wallet = createWallet("io.metamask");
 *           // connect wallet
 *           await wallet.connect();
 *           // return the wallet
 *           return wallet;
 *         })
 *       }
 *     >
 *       Connect
 *     </button>
 *   );
 * }
 * ```
 * @walletConnection
 */
function useConnect(options) {
    return (0, useConnect_js_1.useConnectCore)(index_js_1.connectionManager, options);
}
exports.useConnect = useConnect;
//# sourceMappingURL=useConnect.js.map